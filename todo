the lesson blocks in daily view are not redirecting to lesson_detail.tsx/library/lesson_detail.tsx) when clicked. also modify the ordering of blocks in daily view so that buffer blocks are always in front of the slot
